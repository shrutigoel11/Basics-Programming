/*
Constructor is a special member function that auto initialise the data members of a class upon the creation of objects.
rules and properties.
same name as of class, no return type except void, not called by . opeartor, overloading support, DECLARED IN PUBLIC SECTION;Doesn't support inheritance;
excetued once only;
types: default, parameterised, copy

*/

