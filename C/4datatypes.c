#include <stdio.h>

int main()
{

    /*Decalaring and assigning values to variables.*/

    int age = 19;  //integer datatype stores numerical whole nos.
    double cgpa = 10.0;  //double is kind of decimal type that stores decimal values.
    char grade = 'O';  //character type data which stores single character enclosed in single quotes.
    char name[] = "Shruti"; //bunch of characters is string type data that stores multiple characters.

return 0;
}